<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="notice">

	<!-- parameterType 입력된 값 전달용  -->
	<!-- resultType은 쿼리 실행 결과를 매핑 -->
	
   <!--  게시글 조건  -->
     	<sql id="where-list">
		<choose>
			<when test="condition == 'all' ">
			   (INSTR(subject, #{keyword}) &gt; 0
		          OR DBMS_LOB.INSTR(content, #{keyword}) &gt; 0 )
			</when>
			<when test="condition == 'reg_date' ">
			   (TO_CHAR(reg_date, 'YYYYMMDD') = #{keyword}
		          OR TO_CHAR(reg_date, 'YYYY-MM-DD') = #{keyword} )
			</when>
			<when test="condition == 'content'">
			    DBMS_LOB.INSTR(content, #{keyword}) &gt; 0
			</when>
			<otherwise>
			    INSTR(${condition}, #{keyword}) &gt; 0
			</otherwise>
		</choose>
	</sql>
	
	
	<!--  insert -->	
	<insert id="insertNotice" parameterType="com.sp.grooveware.notice.Notice">      
	    INSERT INTO notice (NOTI_ID, NOTI_TITLE, NOTI_CONTENT, NOTI_REGDATE, NOTI_HITCOUNT, emp_no, dept_id)
	    VALUES (
	        notice_seq.nextval,
	        #{noti_title},
	        #{noti_content},
	        SYSDATE,
	        0,
	        #{emp_no},
	        (SELECT DEPT_NO FROM dept WHERE DEPT_NO = #{dept_no})
	    )
	</insert>
	
	<!--  글 개수 -->
   

        <!-- 공지 글 리스트 -->
    <select id="listNotice" parameterType="map" resultType="com.sp.grooveware.notice.Notice">
	   SELECT noti_id, noti_title, TO_CHAR(noti_regdate, 'YYYY-MM-DD') noti_regdate,
       noti_hitcount, NVL(save_filename, 0) as save_filename, NVL(original_filename, 0) as original_filename
       FROM notice;
    </select>
    <!--  글보기 -->
    <select id="readNotice" parameterType="Long" resultType="com.sp.grooveware.notice.Notice">
   
	
	
	</select>
    
   
    <!--  글수정 -->
   
    <!--  글삭제 -->
    
    
  
</mapper>