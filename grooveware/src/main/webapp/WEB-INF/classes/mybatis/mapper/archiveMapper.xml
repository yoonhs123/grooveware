<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="archive">
	<!-- 폴더 추가 -->
	<insert id="insertFolder" parameterType="com.sp.grooveware.archive.Archive">
		INSERT INTO reference_folder(FOLDER_NO, EMP_NO, FOLDER_NAME, REFERENCE_ROOM_ID)
		VALUES(reference_folder_seq.NEXTVAL, #{emp_no}, #{foleder_name}, #{reference_room_id})
	</insert>
	
	<!-- 파일 추가 -->
	<insert id="insertFile" parameterType="com.sp.grooveware.archive.Archive">
		INSERT INTO reference_file(FILE_NO, FOLDER_NO, SAVE_FILENAME, ORIGINAL_FILENAME, FILE_SIZE)
		VALUES (reference_file_seq.NEXTVAL, 1, #{save_filename},
			#{original_filename}, #{file_size})
	</insert>
	
</mapper> 