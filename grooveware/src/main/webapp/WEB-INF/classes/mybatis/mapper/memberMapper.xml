<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<select id="loginMember" parameterType="String" resultType="com.sp.grooveware.member.MemberDTO">
		SELECT emp_no, emp_name, emp_pwd, emp_rrn, emp_email, emp_tel, emp_address, emp_join_date
		FROM emp 
		WHERE emp_no = #{emp_no}
	</select>

	
</mapper>